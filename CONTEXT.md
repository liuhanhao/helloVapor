# helloVapor IM

一个即时聊天（IM）系统：Vapor（Swift）服务端 + iOS 客户端 + Web 客户端，支持一对一单聊与群聊。

## Language（语言）

**用户（User）**：
注册后可登录系统的账号，拥有昵称、头像和登录凭证。
_Avoid（避免）_: 账号、客户、account

**会话（Session）**：
消息收发的上下文，有两种形态：**单聊**与**群聊**。
_Avoid（避免）_: 聊天室、对话框、channel

**单聊（Direct session）**：
两个用户之间的一对一会话。需要区分形态时才用这个词，平时可直接说「会话」。

**群（Group）**：
一个持久实体：有名称、头像、创建者和成员列表。群本身**不是**会话——它是会话的参与方容器。
_Avoid（避免）_: 聊天室、频道、channel、讨论组、圈子

**群聊（Group session）**：
在一个群内、由其成员共同参与的会话形态。

**成员（Member）**：
群内的用户，显式记录的关系。
_Avoid（避免）_: 用「联系人」指群成员——联系人是由历史消息**推导**出来的，成员是**显式**关系，两者来源不同，混用会让人误以为退群后关系还在

**收件主体（Recipient）**：
消息投递的目标，取值为「某个用户」或「某个群」。消息表用它统一表达消息发往哪里。
_Avoid（避免）_: 收件人、接收方（这两者都暗示是个人）

**已读位点（Read cursor）**：
我在某个收件主体下已读到的位置（时间戳）。未读数由它推出，不单独落库。
_Avoid（避免）_: 已读回执（那是「对方看没看我的消息」，方向相反）；用「红点」「角标」指这个概念（它们是界面元素，不是领域事实）

**消息（Message）**：
会话中发出的一条内容，发往该会话的收件主体，带有消息类型（msgType）。
_Avoid（避免）_: 事件、通知、push

**消息类型（msgType）**：
消息内容的形态分类：text（文本，含 Unicode emoji）、image、audio、video。
_Avoid（避免）_: kind、category

**媒体消息**：
消息类型为 image、audio 或 video 的消息；内容字段存的是服务端文件的 URL，而非文件本身。
_Avoid（避免）_: 附件、文件消息

**撤回（Recall）**：
发送者把自己已发出的消息撤回来，对所有人同时生效。撤回是**软删**——消息行仍在，只是不再对外显示原文，代之以提示文案。
_Avoid（避免）_: 用「删除」指撤回（删除是仅自己不看，另一个方向）；已读回执（那是「对方看没看我的消息」）；「撤销」

**联系人**：
单聊会话中的对方用户，由历史消息推导而来，无需显式的好友关系。
_Avoid（避免）_: 好友、friend；用「联系人」指群成员（那是「成员」）

**token**：
登录成功后签发的访问凭证，后续请求用于标识用户身份。
_Avoid（避免）_: session、cookie
