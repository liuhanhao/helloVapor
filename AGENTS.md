# helloVapor

Vapor（Swift 服务端）+ VaporChat（iOS 客户端）组成的 IM 即时聊天项目。

## Agent skills

### Issue tracker

issue 和规格文档以 Markdown 文件存放于 `.scratch/<feature>/` 下。参见 `docs/agents/issue-tracker.md`。

### Triage labels

使用五个默认分类标签（needs-triage / needs-info / ready-for-agent / ready-for-human / wontfix）。参见 `docs/agents/triage-labels.md`。

### Domain docs

单上下文布局：根目录 `CONTEXT.md` + `docs/adr/`。参见 `docs/agents/domain.md`。
